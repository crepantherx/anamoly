# Render.com configuration
# Deploys ML service only (Frontend is on Vercel)

services:
  # ML Prediction Service  
  - type: web
    name: anomaly-ml
    runtime: docker
    dockerfilePath: ./Dockerfile.ml
    dockerContext: .
    envVars:
      - key: PORT
        value: 8001
